# 🏗️ POS System - Visual Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE LAYER                         │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────┐│
│  │              │  │              │  │              │  │         ││
│  │  POS         │  │  Inventory   │  │  Analytics   │  │  Auth   ││
│  │  Terminal    │  │  Management  │  │  Dashboard   │  │  Pages  ││
│  │              │  │              │  │              │  │         ││
│  │  • Scan      │  │  • Products  │  │  • Charts    │  │  • Login││
│  │  • Cart      │  │  • Stock     │  │  • Reports   │  │  • Signup│
│  │  • Checkout  │  │  • Category  │  │  • Export    │  │         ││
│  │  • Receipt   │  │  • Alerts    │  │              │  │         ││
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └────┬────┘│
│         │                 │                 │                │     │
│         └─────────────────┴─────────────────┴────────────────┘     │
│                                    │                                │
└────────────────────────────────────┼────────────────────────────────┘
                                     │
                    ┌────────────────▼────────────────┐
                    │    CONTEXT & STATE LAYER        │
                    │                                 │
                    │  ┌──────────────────────────┐  │
                    │  │   AuthContext            │  │
                    │  │   • User state           │  │
                    │  │   • Profile & role       │  │
                    │  │   • Sign in/out          │  │
                    │  └──────────────────────────┘  │
                    │                                 │
                    │  ┌──────────────────────────┐  │
                    │  │   CartContext            │  │
                    │  │   • Cart items           │  │
                    │  │   • Calculations         │  │
                    │  │   • Tax & discounts      │  │
                    │  └──────────────────────────┘  │
                    └────────────────┬────────────────┘
                                     │
                    ┌────────────────▼────────────────┐
                    │    API/CLIENT LAYER             │
                    │                                 │
                    │  ┌──────────────────────────┐  │
                    │  │   Supabase Client        │  │
                    │  │   • Auth SDK             │  │
                    │  │   • Database queries     │  │
                    │  │   • Realtime channels    │  │
                    │  └──────────────────────────┘  │
                    └────────────────┬────────────────┘
                                     │
                                     │ HTTPS/WSS
                                     │
┌────────────────────────────────────▼────────────────────────────────┐
│                        BACKEND LAYER (Supabase)                      │
│                                                                      │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │                  │  │                  │  │                  │ │
│  │  Auth Service    │  │  PostgreSQL DB   │  │  Realtime API    │ │
│  │                  │  │                  │  │                  │ │
│  │  • JWT tokens    │  │  • 8 tables      │  │  • WebSockets    │ │
│  │  • Sessions      │  │  • RLS policies  │  │  • Broadcasts    │ │
│  │  • User mgmt     │  │  • Triggers      │  │  • Presence      │ │
│  │                  │  │  • Functions     │  │                  │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘ │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

## Database Schema Relationships

```
                    ┌─────────────┐
                    │   profiles  │
                    │  (users)    │
                    └──────┬──────┘
                           │
                ┌──────────┼──────────┐
                │          │          │
                ▼          ▼          ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │transactions│ │ stock    │  │  audit   │
        │           │  │movements │  │  logs    │
        └─────┬─────┘  └────┬─────┘  └──────────┘
              │             │
              │             │
              ▼             ▼
        ┌──────────┐  ┌──────────┐
        │transaction│ │ products │◄───┐
        │  items    │  │          │    │
        └───────────┘  └─────┬────┘    │
                             │         │
                             ▼         │
                       ┌──────────┐    │
                       │categories│────┘
                       └──────────┘

        ┌──────────┐
        │customers │──────► transactions
        │(loyalty) │
        └──────────┘
```

## Authentication Flow

```
1. User Login
   │
   ├─► Supabase Auth
   │       │
   │       ├─► Generate JWT
   │       │
   │       └─► Create Session
   │
2. Fetch Profile
   │
   ├─► Query profiles table
   │       │
   │       └─► Get role (admin/manager/cashier)
   │
3. Set Auth Context
   │
   └─► App renders with user state

4. Protected Routes
   │
   ├─► Check user session
   │
   ├─► Verify role permissions
   │
   └─► Allow/Deny access
```

## POS Transaction Flow

```
1. SCANNING
   │
   ├─► Barcode scan or manual search
   │
   └─► Find product in database
       │
2. ADD TO CART
   │
   ├─► Update CartContext
   │
   ├─► Calculate totals
   │
   └─► Display in UI
       │
3. CHECKOUT
   │
   ├─► Select customer (optional)
   │
   ├─► Apply discounts
   │
   ├─► Calculate loyalty points
   │
   └─► Choose payment method
       │
4. PROCESS TRANSACTION
   │
   ├─► Create transaction record
   │
   ├─► Create transaction_items
   │
   ├─► Update product stock (trigger)
   │
   ├─► Create stock_movements (trigger)
   │
   └─► Update customer points
       │
5. GENERATE RECEIPT
   │
   ├─► Create PDF with jsPDF
   │
   ├─► Download/Print
   │
   └─► Clear cart
```

## Inventory Update Flow

```
1. STOCK CHANGE
   │
   ├─► Manual adjustment
   │   OR
   ├─► Sale transaction
   │   OR
   └─► Restock operation
       │
2. UPDATE DATABASE
   │
   ├─► Update products.stock_quantity
   │
   └─► Insert stock_movements record
       │
3. TRIGGER REALTIME
   │
   ├─► Supabase broadcasts change
   │
   └─► All connected terminals receive update
       │
4. UPDATE UI
   │
   ├─► Refresh product data
   │
   ├─► Update stock displays
   │
   └─► Show low-stock alerts
```

## Role-Based Access Control

```
┌─────────────┐
│   ADMIN     │  ✓ All Features
└──────┬──────┘
       │
       ├─► User Management
       ├─► Full Inventory Access
       ├─► All Reports
       ├─► Audit Logs
       ├─► System Settings
       └─► Can Refund Transactions
           │
           ▼
    ┌──────────────┐
    │   MANAGER    │  ✓ Inventory + Reports
    └──────┬───────┘
           │
           ├─► Inventory Management
           ├─► Stock Adjustments
           ├─► View All Transactions
           ├─► Generate Reports
           └─► Analytics Dashboard
               │
               ▼
        ┌──────────────┐
        │   CASHIER    │  ✓ Sales Only
        └──────┬───────┘
               │
               ├─► POS Terminal
               ├─► Process Sales
               ├─► View Own Transactions
               └─► Basic Customer Lookup
```

## Security Layers

```
1. Frontend Security
   │
   ├─► Route protection (useAuth hook)
   ├─► Role-based UI rendering
   ├─► Input validation (TypeScript)
   └─► XSS prevention (React escaping)
       │
2. API Security
   │
   ├─► JWT token validation
   ├─► HTTPS only
   ├─► CORS configuration
   └─► Rate limiting (Supabase)
       │
3. Database Security
   │
   ├─► Row Level Security (RLS)
   ├─► Role-based policies
   ├─► SQL injection prevention
   └─► Encrypted connections
       │
4. Audit Trail
   │
   └─► All actions logged in audit_logs table
```

## Offline Support Architecture (Planned)

```
┌─────────────────────────────────────┐
│         Online Mode                 │
│                                     │
│  App ◄──► Supabase (Direct)        │
│                                     │
└─────────────────────────────────────┘

              NETWORK LOST
                   │
                   ▼

┌─────────────────────────────────────┐
│         Offline Mode                │
│                                     │
│  App ◄──► IndexedDB (Local)        │
│             │                       │
│             ├─► Queue transactions │
│             ├─► Cache products     │
│             └─► Store cart         │
│                                     │
└─────────────────────────────────────┘

              NETWORK RESTORED
                   │
                   ▼

┌─────────────────────────────────────┐
│      Background Sync                │
│                                     │
│  Service Worker                     │
│       │                             │
│       ├─► Push queued transactions │
│       ├─► Sync with Supabase       │
│       └─► Clear local cache        │
│                                     │
└─────────────────────────────────────┘
```

## Real-time Multi-Terminal Sync

```
Terminal 1                Terminal 2                Terminal 3
    │                         │                         │
    ├─► Sale: Product A      │                         │
    │   (stock: 10 → 9)       │                         │
    │                         │                         │
    └───────┬─────────────────┼─────────────────────────┤
            │                 │                         │
            ▼                 │                         │
    ┌────────────┐           │                         │
    │  Supabase  │           │                         │
    │  Database  │           │                         │
    └──────┬─────┘           │                         │
            │                 │                         │
    ┌───────┴─────────────────┴─────────────────────────┴────┐
    │         Realtime Broadcast (WebSocket)                  │
    └───────┬─────────────────┬─────────────────────────┬────┘
            │                 │                         │
            ▼                 ▼                         ▼
    Product A: 9      Product A: 9            Product A: 9
    (updated)         (updated)               (updated)
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Repository                     │
│                  (Source Code + CI/CD)                   │
└────────────┬────────────────────────────────────────────┘
             │
             │ Push/Merge
             │
    ┌────────▼────────┐
    │  GitHub Actions │
    │   (CI/CD)       │
    │  • Tests        │
    │  • Build        │
    │  • Deploy       │
    └────────┬────────┘
             │
    ┌────────┴────────────────────┐
    │                             │
    ▼                             ▼
┌─────────┐                 ┌──────────┐
│ Vercel  │                 │ Supabase │
│         │                 │  Cloud   │
│ • Next  │◄───────────────►│          │
│ • Edge  │    API Calls    │ • DB     │
│ • CDN   │    (HTTPS)      │ • Auth   │
│         │                 │ • RT     │
└─────────┘                 └──────────┘
     │
     │ HTTPS
     │
     ▼
┌─────────┐
│  Users  │
│ Browser │
└─────────┘
```

---

**This architecture is designed for:**
- ⚡ High performance
- 🔄 Real-time updates
- 🔒 Security
- 📱 Mobile support
- 🌐 Offline capability
- 📈 Scalability

---

*POS System Architecture v1.0*
*Last Updated: December 22, 2025*
